{
  "mcpServers": {
    "unraid-mcp": {
      "type": "http",
      "url": "${user_config.unraid_mcp_url}/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.unraid_mcp_token}"
      }
    }
  }
}
