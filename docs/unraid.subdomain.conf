server {
    listen 443 ssl;
    server_name unraid.*;
    include /config/nginx/ssl.conf;
    location / {
        include /config/nginx/proxy.conf;
        proxy_pass http://unraid-mcp:3000;
    }
}
